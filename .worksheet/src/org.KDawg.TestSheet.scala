package org.KDawg
import scala.concurrent.duration._
import gui.GridBagDemo.TimerWindowRows

object TestSheet {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(155); 
  println("Welcome to the Scala worksheet");$skip(20); 
  val a = 5 seconds;System.out.println("""a  : scala.concurrent.duration.FiniteDuration = """ + $show(a ));$skip(20); 
  val b = 6 seconds;System.out.println("""b  : scala.concurrent.duration.FiniteDuration = """ + $show(b ));$skip(22); 
  
  println( a < b )}
  
  import java.util.Date
  
  
  
}
